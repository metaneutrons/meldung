# next-auth@5-beta pulls @auth/core, which declares peerOptional nodemailer@^7.
# This app uses nodemailer@^8 directly for delivery (not next-auth's Email
# provider), so that optional peer does not apply. Accept the resolution so
# `npm install` behaves like the lockfile (built the same way) and CI stays green.
legacy-peer-deps=true
